export enum DBCollection {
  User = 'User',
  Production = 'Production',
  Comment = 'Comment',
  CartUser = 'CartUser',
  Category = 'TypeProduct',
  Bill = 'Bill',
}

export const DataBaseName = 'tc-store';

export enum MathDB {
  $in = '$in',
  $gt = '$gt',
  $gte = '$gte',
  $lt = '$lt',
  $nor = '$nor',
  $and = '$and',
  $or = '$or',
  $where = '$where',
}
